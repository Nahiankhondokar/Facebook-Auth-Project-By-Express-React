
// user reducer
const UserReducer = (state, { type, payload }) => {

    switch (type) {
        case '':
        return '';

    
        default:
            return state;
    }

}

export default UserReducer;